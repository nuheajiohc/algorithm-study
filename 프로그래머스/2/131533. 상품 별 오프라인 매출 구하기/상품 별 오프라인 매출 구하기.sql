-- 코드를 입력하세요
SELECT p.PRODUCT_CODE, SUM(os.SALES_AMOUNT) * p.PRICE AS SALES FROM PRODUCT AS p JOIN OFFLINE_SALE AS os ON p.PRODUCT_ID = os.PRODUCT_ID GROUP BY os.PRODUCT_ID ORDER BY SALES DESC, p.PRODUCT_CODE;